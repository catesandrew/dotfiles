--- 
wget: |-
  wget <url>
  	Download a file.
  
  wget -E -H -k -K -p <url>
  	Get all files required to display the page locally.
  
  wget -q -0 - <url>
  	Download <url> and print content to stdout
