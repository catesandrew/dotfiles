#!/bin/bash

# ToDo:
# - Consider all options: python -m premailer --help
# - Reconsider '--remove-classes'

BASH_NO=no . /etc/profile

export PYTHONIOENCODING=utf-8
# python -m premailer --remove-classes --strip-important --disable-validation --pretty
python -m premailer --disable-validation --pretty
